# TODO
- [ ] Add one-time initialization guard in `components/editor/BlogEditor.tsx` to stop the Maximum update depth exceeded loop.
- [ ] Re-run Next.js dev server and verify console error is gone.
- [ ] Smoke test: type in editor; ensure content is not overwritten after initial load.

